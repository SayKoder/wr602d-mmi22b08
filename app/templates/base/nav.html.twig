    <body>
        <header>
            <nav class="navbar navbar-expand-lg navbar-dark navbar-custom">
                <div class="container">
                    <a class="navbar-brand" href="{{ path('accueil') }}">Kevinitator</a>
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="navbarNav">
                        <ul class="navbar-nav ms-auto align-items-center">
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('accueil') }}">Accueil</a>
                            </li>
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                  Gérer Pdf
                                </a>
                                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                                    {% if app.user %}
                                        <a class="dropdown-item" href="{{ path('generate_pdf') }}">Générer un pdf</a>
                                        <a class="dropdown-item" href="{{ path('historique') }}">Historique</a>
                                        <div class="dropdown-divider"></div>
                                        <a class="dropdown-item" href="{{ path('app_subscription') }}">Abonnement</a>
                                    {% else %}
                                        <a class="dropdown-item" href="{{ path('app_login') }}">Générer un pdf</a>
                                        <div class="dropdown-divider"></div>
                                    <a class="dropdown-item" href="{{ path('app_login') }}">Abonnement</a>
                                    {% endif %}
                                </div>
                            </li>
                            {% if app.user %}
                                <li class="nav-item dropdown">
                                    <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                        <img src="{{ asset('assets/profil.png') }}" alt="Profil" style="width: 60px; height: 60px;">
                                    </a>
                                    <div class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                                        <a class="dropdown-item" href="{{ path('app_profil') }}">Profil</a>
                                        <a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a>
                                    </div>
                                </li>
                            {% else %}
                                <li class="nav-item">
                                    <a class="nav-link" href="{{ path('app_login') }}">
                                        <img src="{{ asset('assets/profil.png') }}" alt="Profil" style="width: 60px; height: 60px;">
                                    </a>
                                </li>
                            {% endif %}
                        </ul>
                    </div>
                </div>
            </nav>
        </header>
    
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    </body>
